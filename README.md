# Esports Betting MCP

Esports data integration for betting analytics.

## Features
- Data fetching for major esports titles
- Analysis tools

## Setup
1. Clone the repository
2. Install dependencies: `npm install`
3. Configure environment variables in `.env`
